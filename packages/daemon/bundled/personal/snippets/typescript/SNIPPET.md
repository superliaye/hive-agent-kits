---
name: typescript
description: TypeScript conduct — no `any`, no `as any`, no `as unknown as`
tags: [voice, typescript]
---

# TypeScript Rules

- Alert on `any` usage. If no straightforward fix, ask user.

- Forbidden: `as any`, `as unknown as` - must fix, no exceptions.
